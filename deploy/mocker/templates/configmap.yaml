apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app: {{ .Release.Name }}
data:
  MOCKER_ENVIRONMENT: {{ .Values.config.environment | quote }}
  MOCKER_DEBUG: {{ .Values.config.debug | quote }}
