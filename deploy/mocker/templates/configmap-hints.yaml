{{- if .Values.customHints.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-custom-hints
  labels:
    app: {{ .Release.Name }}
data:
  custom_hints.yaml: |
    {{- .Values.customHints.content | nindent 4 }}
{{- end }}
