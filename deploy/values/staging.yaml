replicaCount: 2

image:
  repository: registry.example.com/mocker
  pullPolicy: Always

config:
  environment: staging
  debug: "false"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: mocker.staging.example.com
      paths:
        - path: /
          pathType: Prefix
